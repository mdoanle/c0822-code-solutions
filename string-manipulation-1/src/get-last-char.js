/* exported getLastChar */
function getLastChar(string) {
  var lastChar = string.length - 1;
  return string[lastChar];
}

/*
1)Look at word
2) Grab the last word
3) Show me it

PC
1) variable to represent last index (.length - 1)
2) return string[lastIndex]
*/
