/* exported getIndexes */
function getIndexes(array) {
  var indexes = [];
  for (var i = 0; i < array.length; i++) {
    indexes.push(i);
  }
  return indexes;
}

/*
1) count how many items are in the array.
2) Print out the number of items found in said array
3) Iteration?

Pseudo-Code
1) Output array
2) For loop (i < array.length?)
3) Add current index to the output array
4) Return output.
*/
